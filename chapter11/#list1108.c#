#include<string.h>

size_t strlen(const char *s)
{
  size_t len = 0;

  while(*s++)
    len++;

  return 0;
}

int main(void)
{
  char a[128];

  printf("文字列を入力してください：");
  scanf("%s",a);

  printf("入力した文字列の長さは%dです。",strlen(a));

  return 0;
}